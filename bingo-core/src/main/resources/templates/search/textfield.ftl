[#ftl]
<div class="form-group search-input">
	<input id="${tag.id}" name="${tag.name}" type="text" alt="${tag.label!}" placeholder="${tag.label!}" maxlength="${tag.maxlength!}" class="form-control">
</div>
 